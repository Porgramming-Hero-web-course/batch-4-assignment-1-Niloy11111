{
  //

  function sumArray(nums: number[]): number {
    const sum = nums.reduce((acc, sum) => acc + sum, 0);
    return sum;
  }

  // const problem1 = sumArray([1, 2, 3, 4, 5]);
  // console.log(problem1);

  //
}
